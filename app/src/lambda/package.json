{
  "name": "lambda",
  "version": "1.0.0",
  "description": "Here is the folder to build endpoints for the app to be ulitimately served in lambda",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "lite-utils": "0.0.1"
  }
}
